actions:
  - name: Test
    container_image: ubuntu-20.04
    triggers:
      push:
        branches:
          - "foo"
      pull_request:
        branches:
          - "master"
    bazel_commands:
      - run //:setup_ci --config=workflows --cache_computed_file_digests=500000
      - --bazelrc=custom.bazelrc build //... --config=workflows --build_tag_filters=-no-bazelci --repo_env=FOO=BAR --cache_computed_file_digests=500000
      - --bazelrc=custom.bazelrc test //server/util/... --config=workflows --build_tag_filters=-no-bazelci --repo_env=FOO=BAR --build_tests_only --cache_computed_file_digests=500000
      - run //:setup_ci --config=workflows --cache_computed_file_digests=500000
      - --bazelrc=custom.bazelrc build //... --config=workflows --build_tag_filters=-no-bazelci --repo_env=FOO=BAR --cache_computed_file_digests=500000
      - --bazelrc=custom.bazelrc test //server/util/... --config=workflows --build_tag_filters=-no-bazelci --repo_env=FOO=BAR --build_tests_only --cache_computed_file_digests=500000
      - run //:setup_ci --config=workflows --cache_computed_file_digests=500000
      - --bazelrc=custom.bazelrc build //... --config=workflows --build_tag_filters=-no-bazelci --repo_env=FOO=BAR --cache_computed_file_digests=500000
      - --bazelrc=custom.bazelrc test //server/util/... --config=workflows --build_tag_filters=-no-bazelci --repo_env=FOO=BAR --build_tests_only --cache_computed_file_digests=500000
      - run //:setup_ci --config=workflows --cache_computed_file_digests=500000
      - --bazelrc=custom.bazelrc build //... --config=workflows --build_tag_filters=-no-bazelci --repo_env=FOO=BAR --cache_computed_file_digests=500000
      - --bazelrc=custom.bazelrc test //server/util/... --config=workflows --build_tag_filters=-no-bazelci --repo_env=FOO=BAR --build_tests_only --cache_computed_file_digests=500000
      
plugins:
  - path: cli/plugins/go-deps
  - path: cli/plugins/open-invocation
  - path: cli/plugins/notify
  - path: cli/example_plugins/go-highlight
  - path: cli/example_plugins/ping-remote

